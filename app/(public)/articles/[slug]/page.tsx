import { notFound } from "next/navigation";

/**
 * PLACEHOLDER — reserved for Phase 5 (Content). Since no article data
 * source exists yet, every slug currently 404s rather than pretending
 * to render a real article.
 */
export default function ArticleDetailPage() {
  notFound();
}
